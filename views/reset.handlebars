	<div class="unix-login">
		<div class="container">
			<div class="row">
              <div class="col-lg-7 hidden-xs hidden-sm">
                        <img src="/images/Twiter.jpg" class="img-responsive">
				</div>		
                
                
			                <div class="col-lg-5">
                    <div class="col-lg-12">

          {{#if success_msg}}
            <div class="alert alert-success text-center">{{success_msg}}</div>
          {{/if}}

          {{#if error_msg}}
            <div class="alert alert-danger text-center">{{error_msg}}</div>
          {{/if}}

          {{#if error}}
            <div class="alert alert-danger text-center">{{error}}</div>
          {{/if}}
    </div>
					<div class="login-content">
						<div class="login-logo">
							<a href="/"><span>Promoworks</span></a>
						</div>
						<div class="login-form">
							<h4>Administrator Login</h4>
							<form action = "/reset/{{token}}" method="post">
								<div class="form-group">
									<label>Passsword</label>
									<input type="text" class="form-control" name='password', value='', placeholder='New password', autofocus=true>
								</div>
								<button type="submit" class="btn btn-primary btn-flat m-b-30 m-t-30">Update Password</button>
							</form>
                                <div class="register-link m-t-15 text-center">
                                      	<p>Back to <a href="/"> Home</a></p>
                                </div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>